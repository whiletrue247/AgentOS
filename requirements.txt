# Core
networkx>=3.0
pyyaml>=6.0
httpx>=0.27
rich>=13.0

# LLM Gateway
litellm>=1.40

# Multi-Agent Orchestration
langgraph>=0.2

# Knowledge Graph
neo4j>=5.20
mem0ai>=0.1

# Computer Use & Desktop Automation
playwright>=1.45
pyautogui>=0.9; sys_platform != "linux"

# Hardware Detection (optional)
# torch  — install separately per platform
# onnxruntime — install separately per platform

# Testing
pytest>=8.0
pytest-asyncio>=1.0
pyflakes>=3.0
